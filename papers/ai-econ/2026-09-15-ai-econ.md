# AI Economics Papers — 2026-09-15

No qualifying papers today, and today's run is a broken-harvest day, not an ordinary quiet one. The arXiv preflight check succeeded (HTTP 200 on a generic `all:` query), but the actual Step 2 harvest query — `cat:econ.GN`, `cat:econ.TH`, `cat:cs.GT`, `cat:cs.CY`, individually and combined, at every `max_results` size tried — returned HTTP 429 "Rate exceeded" on every one of roughly 20 attempts spread over more than 25 minutes with exponential backoff up to 3 minutes, while control queries (`all:test`, `cat:cs.AI`) succeeded on the same host in the same window. No arXiv econ/game-theory/CS-and-society data could be retrieved at all today, so nothing from the required harvest categories informed this digest. Given the arXiv outage, Step 3 web-search supplementation (4 searches, within budget) was run as the primary fallback, but every candidate it surfaced was either already covered in an earlier digest or fell outside the 30-day window (published/updated since 2026-08-16); no new, in-window, qualifying paper was found.

---

**Sourcing note:** arXiv API preflight returned 200, but every Step 2 harvest request against the required categories (`econ.GN`, `econ.TH`, `cs.GT`, `cs.CY`) returned HTTP 429 "Rate exceeded," including single-category, minimal-`max_results`, and alternately-encoded variants, both immediately and after waits up to 180 seconds — this was isolated as specific to those categories, since `all:test` and `cat:cs.AI` queries succeeded throughout. HARVEST_STATS could not be produced (no harvest data returned). Both NBER RSS feeds were unusable in the same way as recent prior editions: `nber.org/papers.rss` returns HTTP 301 → 404, and `nber.org/programs/io/papers.rss` returns HTTP 200 with an HTML meta-refresh redirect to a landing page, not a feed. Step 3 web search (4/4 searches used) surfaced NBER w34859 ("Chaining Tasks, Redefining Work," issued February 2026), NBER w34984 ("Artificial Intelligence, Productivity, and the Workforce," issued March 2026), and CESifo WP 11459 ("AI Adoption Among German Firms," 2024) — all confirmed via direct page fetch to be outside the 30-day window — plus CEPR DP21571 ("Measuring the AI Economy"), whose page returned HTTP 403 Forbidden and so could not be date-verified (4/4 page fetches used; none spent on already-known duplicates below).

**Excluded, borderline:**
- NBER w34859, "Chaining Tasks, Redefining Work: A Theory of AI Automation" (Demirer, Horton, Immorlica, Lucier, Shahidi) — strong theoretical/economic content (task-chaining model of AI automation, CES aggregation), but issued February 2026, outside the 30-day window, with no revision date shown.
- NBER w34984, "Artificial Intelligence, Productivity, and the Workforce: Evidence from Corporate Executives" (Baslandze et al.) — issued March 2026, outside the 30-day window.
- CESifo WP 11459, "AI Adoption Among German Firms" (Licht, Wohlrabe) — dated 2024, well outside the window.
- CEPR DP21571, "Measuring the AI Economy" — page returned HTTP 403, so date and full content could not be verified; excluded per the anti-fabrication rule rather than guessed.
- NBER w35141 ("The Microstructure of AI Diffusion") and BIS WP 1325 / EIB ("AI adoption, productivity and employment: Evidence from European Firms") — directly on-topic but already featured in earlier editions of this digest, so excluded again as duplicates.

**Unreachable sources:** arXiv Step 2 harvest (all `cat:` queries in the required categories returned HTTP 429 throughout the session, a reproducible block distinct from ordinary rate limiting — see sourcing note); `nber.org/papers.rss` (HTTP 301 → 404); `nber.org/programs/io/papers.rss` (HTTP 200, but redirects to a non-feed HTML page); `cepr.org/publications/dp21571` (HTTP 403 Forbidden).
